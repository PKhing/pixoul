package entity.base;

/**
 * The DispatchAction is the list of action 
 * that {@link Player} can do
 */
public enum DispatchAction {
	USE_ITEM, UNEQUIP, SWITCH_EQUIP, ATTACK, MOVE_LEFT, MOVE_RIGHT, MOVE_UP, MOVE_DOWN, STAY_STILL, DELETE_ITEM,
}
